\* Nightly / optional heavy bound — same Spec as OrchidLog.tla (partitions, nack competitor, forget).
\* Hard gate remains OrchidLog.cfg / OrchidLog-fast.cfg (MaxSeq=3).
CONSTANTS
  n1 = n1
  n2 = n2
  n3 = n3
  MaxSeq = 5

SPECIFICATION Spec

INVARIANTS
  TypeOK
  SingleSlotAgreement
  LogPrefix
  NoForkEq
  DurableBeforeAdvance
  ConfiguredQuorumFixed